[pytest]
minversion = 8.0
testpaths = tests
asyncio_mode = auto
addopts = -ra --strict-markers
markers =
    unit: unit tests with mocked dependencies
    integration: end-to-end pipeline tests
    security: prompt injection and auth tests
